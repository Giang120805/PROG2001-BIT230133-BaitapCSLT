#include <stdio.h>

int main() {

    // ===== 3 tên biến hợp lệ =====

    int a = 5;
    // Hợp lệ: bắt đầu bằng chữ cái

    int so1 = 10;
    // Hợp lệ: có thể chứa số nhưng không bắt đầu bằng số

    float pi = 3.14;
    // Hợp lệ: tên biến bắt đầu bằng chữ cái và số thực dùng dấu '.'


    // ===== 3 tên biến không hợp lệ =====

    // int 1a = 5;
    // Không hợp lệ: bắt đầu bằng số

    // int so-1 = 10;
    // Không hợp lệ: có dấu '-' (ký tự đặc biệt)

    // float pi = 3,14;
    // Không hợp lệ: số thực phải dùng dấu '.' không dùng ','

    return 0;
}